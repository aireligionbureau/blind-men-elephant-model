# 参与贡献

感谢你愿意帮助完善盲人摸象模型。本项目不仅检查代码是否运行，也检查一项改动是否保留了可追溯的认知链、证据边界和真相轮廓质量。

## 本地准备

需要 Python 3.10 或更高版本。克隆仓库后，在虚拟环境中执行：

```bash
python -m pip install --editable ".[test]"
python tools/run_plain_tests.py
```

测试不需要 API key，也不会访问外部模型或搜索服务。真实运行所需的密钥只能放在环境变量中；不要提交 `.env`、运行目录、日志或原始授权响应。

## 改动原则

- 保持数字人的九维认知身份、信息过滤器与证据账本可追溯。
- 不允许 mock、旧题缓存或未核验搜索摘要静默冒充真实证据。
- 不以增加字段、提示词长度和调用次数代替能力提升。
- 任何性能优化都必须守住法医层、侦探层和轮廓层的质量门槛。
- 用户可见结论应直接回答具体问题，并用大白话说明阴影出现在哪里、怎样影响判断。
- 新增经典透镜时，必须写明适用边界和防误用规则；经典不能替代现实证据。

## 提交 Pull Request

1. 保持改动聚焦，一个 PR 解决一个清楚的问题。
2. 为行为变化补充或更新测试。
3. 先运行 `python tools/run_plain_tests.py`。
4. 说明效果、耗时、模型调用次数和不回归依据。
5. 涉及真实运行材料时，只提交经过脱敏、确有复核价值的小型基准摘要。

提交信息建议使用简短的祈使句，例如 `Fix stale run recovery` 或 `Add contour provenance gate`。
